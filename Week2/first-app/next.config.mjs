// next.config.js
/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
      domains: ["assets.aceternity.com"], // add other domains if needed
    },
  };
  
  export default nextConfig;